spring.datasource.url=jdbc:sqlserver://34.67.61.32:1433;database=cafateria;encrypt=true;trustServerCertificate=false;host=34.67.61.32;loginTimeout=30

# Set database credentials securely (not recommended in properties file for production)
spring.datasource.username=cafateria
spring.datasource.password=Cafateri@24

# Recommended approach for production: Use environment variables for credentials
# spring.datasource.username=${MYSQL_USERNAME}
# spring.datasource.password=${MYSQL_PASSWORD}

# Optional: Configure connection pool properties
spring.datasource.hikari.poolName=MyHikariPool
spring.datasource.hikari.minimumIdle=5
spring.datasource.hikari.maximumPoolSize=10
spring.datasource.hikari.idleTimeout=600000
spring.datasource.hikari.maxLifetime=1800000

# Additional driver class configuration (if needed)
# spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
